"""Static report: self-contained HTML (embedded Plotly.js, zero external URLs)
plus per-figure PNGs. Static export requires kaleido and an installed
Chrome/Chromium, verified at startup with a clear error.
"""
