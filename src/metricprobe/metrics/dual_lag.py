"""Dual lag: completion curves on both source-side insert time and local load
time, plus the per-row delta separating upstream provider lag from local
ingestion lag. NULL source timestamps form their own reported bucket.
"""
