"""Parity between two named probes: FULL OUTER JOIN over event months on the
common mature population, watermarked by as_of; prerequisites verified per run,
else INDETERMINATE — never a false mismatch.
"""
